\documentclass{scrbook}

\usepackage{fontspec}
\usepackage{polyglossia}
\usepackage{amsmath}
\usepackage{unicode-math}
\usepackage{hyperref}
\usepackage{graphicx}
\usepackage{bookmark}

\setmathfont{$mathfont$}

$if(pagestyle)$
\pagestyle{$pagestyle$}
$endif$

$if(header)$
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhead[L]{$header$}
$endif$

$if(footer)$
\fancyfoot[C]{$footer$}
$endif$

$if(title)$
\title{$title$}
$endif$

$if(author)$
\author{$author$}
$endif$

\begin{document}

$if(title)$
\maketitle
$endif$

$if(toc)$
\tableofcontents
$endif$

$body$

\end{document}